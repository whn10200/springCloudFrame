##端口
server:
  port: 10007

##应用名  
spring:
  application:
    name: bp-api-facade
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:10006/

##日志
logging:
  config: classpath:logback-spring.xml
  path: F:/log/bp/apifacade/
  level:
        com.tuodao.bp.api.facade: info